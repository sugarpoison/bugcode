# Numeric values don't get quotation marks.
jake = 12
mary = 15
total_apples = jake + mary
print("The two of them have ", total_apples, "apples")

my_list = [144, 72, 23, 199]
# Ranges should not exceed the values in a list.
for i in range(0, 4):
    print(my_list[i])

# Assign variables before you try to use them.
my_variable = "Smart!"
print(my_variable)


